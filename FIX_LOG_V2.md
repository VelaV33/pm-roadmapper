# Roadmap OS Fix Log v2 — 2026-04-13

Auto-generated by the autonomous fix runner v2.

## Pre-flight Findings

- **Dark mode mechanism:** Class-based `.dark-mode` on `<body>`. Variables at lines 38-52. ~60 dark-mode CSS overrides already exist (lines 53-97+). Key vars: `--bg:#0f172a`, `--white:#1e293b`, `--text:#e2e8f0`, `--muted:#94a3b8`, `--border:#334155`, `--surface-low:#1e293b`, `--surface-high:#334155`
- **Hardcoded white backgrounds:** 397 instances of `background.*#fff|white|#ffffff|#fafafa|#f5f5f5` — the root cause of all dark mode contrast issues
- **Routing:** No `showPage()` function. Each feature uses its own open/close pattern (openPlans, openG2M, openCapacity, openTodo, etc.)
- **Renderer size:** 23,500+ lines

---

## Fix 1: Global Dark Mode Overhaul
**Status:** DONE
- Added ~80 lines of comprehensive dark mode CSS overrides after line 127
- Broad selectors cover: overlay headers (.g2m-header, .todo-header, .art-header, .cap-topbar, .plans-topbar, .prio-topbar), modal surfaces, form inputs, buttons, tables, cards, toolbar bars
- Inline hardcoded colour catch-alls for background:#fff, white, #fafafa, #f5f5f5, #e8f4fd, #f8faff, etc.
- Specific fixes: CapacityIQ dashboard, Plans/Gantt view toggles, G2M product description, To-Do kanban columns, Edit Row modal separators, Feedback overlay
- All overlay title bars now use `rgba(15,23,42,.92)` dark glassmorphic background

## Fix 2: Global Dropdown Dark Mode
**Status:** DONE
- Added `body.dark-mode select, select option` overrides
- Fixed .row-kebab-menu dark mode: dark background, border, shadow, hover states
- Danger menu items now use #f87171 text on #7f1d1d hover background

## Fix 5: To-Do Page UI Overhaul
**Status:** DONE
- Updated .todo-item: 12px border-radius, 14px padding, box-shadow, hover accent border
- Updated .kanban-col: added box-shadow
- Updated .kanban-card: 12px radius, 14px padding, accent hover border
- Updated .todo-comment-btn: proper border-radius, hover states with accent color
- Dark mode for done items uses var(--surface-low) instead of hardcoded #f8faf8

## Fix 6: Initiative Linking
**Status:** DONE
- Added `initiativeLinks` field to row data model
- Added "Linked Initiatives" section in Edit Row modal with picker (searches own initiatives)
- Link types: Dependency / Reference with coloured badges
- Status dot shows initiative health (green/blue/amber/red based on bar colours)
- Links saved and persisted with row data
- TODO: Backend support for cross-roadmap linking from shared roadmaps

## Fix 7: Section Editing
**Status:** DONE
- Added "Edit Section" button (edit icon) to section header actions
- Opens a modal with: Name, Description (new field), Colour picker, Owner (new field)
- Saves back to section data and re-renders
- All fields use CSS variables for dark mode compatibility

## Fix 8: CapacityIQ Dashboard Fix
**Status:** DONE (already working)
- Dashboard was already rendering correctly — `openCapacity()` calls `capNav('dashboard')` which calls `renderCapDashboard()`
- Header dark mode covered by Fix 1 global overrides (`.cap-topbar` glassmorphic dark)
- No additional code changes needed

## Fix 9: Task Library Bulk Upload
**Status:** DONE
- Added `downloadLibraryTemplate()` function that generates a .xlsx template
- Added browser fallback for file upload (hidden file input when Electron API unavailable)
- Added `_processLibraryWorkbook()` shared function for both Electron and browser paths
- Template download button added to Task Library toolbar
- Cross-module population was already implemented (ToDo, Plans, G2M all call upsertTaskInLibrary)
- Enhanced column detection to also find Category and Hours columns

## Fix 10: G2M Hours Input
**Status:** DONE
- Added CSS to shrink browser native number stepper buttons (16px height, 12px width)
- Used -moz-appearance:textfield for Firefox
- Column width constrained to 50px for cleaner layout
- Input still accepts free-text numeric entry

## Fix 11: Plans/Gantt Dark Mode
**Status:** DONE
- Added alternating row dark mode colours (even: surface-low, odd: white)
- Plans table inputs and selects get dark mode background/color/border
- Title bars and headers already covered by Fix 1 global overlay overrides

## Fix 12: User Avatar Circle
**Status:** DONE
- Profile button now shows circular avatar with user initials
- 28px circle, accent background, white text, 11px bold font
- First name displayed next to avatar with text-overflow ellipsis
- Falls back to first letter if single name

## Fix 13: Move Help Button
**Status:** DONE
- Removed Help button from top-right toolbar header
- Added "Help & Support" section to Settings modal
- Includes "Open Help Guide" and "Replay Onboarding Tour" buttons
- Both use svgIcon with proper dark mode styling

## Fix 14A: Artifacts Button Colors
**Status:** DONE
- Replaced Brand Guide pink/gradient button with theme accent colour
- Now uses `var(--accent)` background with white text
- Consistent with other accent buttons throughout the app

## Fix 14B: Document Repository
**Status:** DONE
- Added `documentRepository` global array for document storage
- New section rendered at bottom of Artifacts page with folder grouping
- Features: folder names, document cards, initiative linking, tags, search
- Add Document modal with: name, folder, initiative picker, tags, description
- Data persisted via `persistData()` and loaded on restore
- Dark mode support via CSS variables
- TODO: File upload to Supabase Storage (backend not yet implemented)

## Fix 15: G2M Emoji + Insights Dark Mode
**Status:** VERIFIED
- G2M header dark mode covered by Fix 1 (global overlay header overrides)
- All G2M emoji replaced in Fix 3 (svgIcon system)
- G2M insights rendered within G2M overlay, inherits dark mode from parent

## Fix 16: Artifacts Templates Emoji
**Status:** VERIFIED
- All ART_TYPES icons changed from emoji to SVG icon names in Fix 3
- Template card rendering updated to use svgIcon()
- Type badge rendering uses svgIcon()
- Select dropdown options use plain text names (no emoji)

## Fix 4: To-Do Task Reuse (Cross-Module Autocomplete)
**Status:** DONE
- Added `searchAllModuleTasks()` function that aggregates tasks from Library, ToDo, Plans, and G2M
- Enhanced `attachLibraryAutocomplete()` to use cross-module search
- Source badges (Library/ToDo/Plans/G2M) with distinct colors displayed in dropdown
- Hours auto-fill from matched entries
- Deduplication by task name (case-insensitive)

## Fix 3: Emoji to SVG Replacement
**Status:** DONE
- Created `svgIcon()` helper function with 50+ SVG icon paths (stroke-based, 24x24 viewBox)
- Replaced ALL Unicode emoji (1F300-1FAFF range, plus extended) with either:
  - SVG inline icons (via svgIcon() for JS contexts)
  - Static inline SVGs (for HTML contexts)
  - Clean text labels (for option elements and small contexts)
- Major replacements: overlay headers, modal headings, action buttons, badges, stat cards
- Updated data arrays: FLEET_RESEARCH_AREAS, GENERIC_RESEARCH_AREAS, ART_TYPES, TOUR_STEPS, RPT_REPORTS, colDefs (kanban), help guide content
- CapacityIQ dashboard stat cards now use SVG icons instead of emoji
- Remaining symbols (star, check, x, pencil, cloud) are ASCII/text symbols, not emoji

---

## Platform Admin Page (2026-04-09)
**Status:** DONE

### Backend
- `supabase/functions/_shared/auth.ts`: Added `isPlatformAdmin()` helper — checks `user.app_metadata.platform_admin === true`
- `supabase/functions/admin-api/index.ts`: Imported `isPlatformAdmin`; `check-role` now returns `is_platform_admin`; access gate changed from `isSuperAdmin(user)` to `isSuperAdmin(user) || isPlatformAdmin(user)`

### Frontend (renderer/index.html)
- Added `_isPlatformAdmin` global flag
- `checkSuperAdmin()` now also checks JWT `app_metadata.platform_admin` client-side and server `is_platform_admin` response; shows `platformAdminSidebarBtn` when true
- New overlay `#platformAdminOverlay` with 3 tabs: **User Directory** (table with search, role dropdown, detail expand), **System Stats** (stat cards: total users, 7-day signups, by-role, by-tier), **Team Management** (lists teams + members)
- Sidebar: "Platform Admin" button with star SVG icon, hidden by default, shown for platform admins
- `closeAllOverlays()` updated to include `platformAdminOverlay`
- `openAdmin()` now also allows `_isPlatformAdmin` users
- CSS: `.pa-tab-bar`, `.pa-tab`, `.pa-table`, `.pa-search`, `.pa-role-sel`, `.pa-stat-grid`, `.pa-stat`, `.pa-detail-card` — all with dark mode overrides
- Dark mode: overlay header override added for `#platformAdminOverlay>div:first-child`

## Microsoft/Outlook OAuth (2026-04-09)
**Status:** DONE
- Added Microsoft OAuth button (`#authMicrosoftBtn`) after the Google button in the auth card
- CSS: `.auth-btn.microsoft` — matches Google button style with Microsoft branding (transparent bg, border, flex layout)
- Handler: `handleMicrosoftAuth()` calls `_supabase.auth.signInWithOAuth({ provider: 'azure' })`
- Microsoft 4-color logo SVG inline in the button
- Dark mode: inherits from `.auth-btn.microsoft` styles (rgba backgrounds work in both modes)

## Organizations Layer (2026-04-13)
**Status:** DONE

### Database Migration
- Created `supabase/migrations/20260413000000_organizations.sql`
- New `organizations` table: id, name, domain, logo_url, created_by, created_at, updated_at
- Unique constraint on name, index on lower(domain)
- RLS: admins read all, members read own org, platform admins manage all
- Added `organization_id` column to `user_profiles` with FK + index
- Updated_at trigger on organizations

### Edge Function (admin-api)
- Added 5 new actions: `list-organizations`, `create-organization`, `update-organization`, `delete-organization`, `assign-user-org`
- Enriched `list-users` response with `organization_id` and `org_name` fields

### Platform Admin UI — Organizations Tab
- 4th tab "Organizations" in Platform Admin overlay
- Create Organization form (name + domain)
- Stats grid: org count, affiliated users, unaffiliated users
- Org cards: name, domain, user count, created date, edit/delete buttons
- Auto-detection note: "Users signing up with @domain will be auto-assigned"
- Expandable member list per org with Add User picker and Remove button
- Unaffiliated Users section at bottom with quick-assign buttons
- Inline edit for org name/domain, confirmation dialog for delete
- Assign-to-org modal for unaffiliated users

### User Directory Enhancements
- Added "Organization" column to PA user table
- Added "Organization" filter dropdown (All / Unaffiliated / specific orgs)
- Shows org name or "--" for unaffiliated users

### Auto-detect Organization on Signup
- In `onAuthSuccess()`, after 2s delay, checks if user has no org
- If email domain matches an org's domain, auto-assigns via PATCH to user_profiles
- Silent failure (console.warn only)

### Dark Mode
- All new CSS classes use CSS variables: `--white`, `--bg`, `--border`, `--text`, `--muted`, `--accent`
- Dark mode overrides for form inputs, inline edit fields, org filter dropdown, remove button hover
- Assign-org modal uses `var(--white)` background, inherits dark mode

---

## Enhanced Signup, Usage Tracking, Subscription Enforcement (2026-04-09)

### Task 1: Enhanced Signup Form
- Added Full Name, Company/Organisation, Phone Number, Designation fields to signup form (hidden in login/forgot/reset modes)
- `switchToSignup()` shows `#authSignupFields`; `switchToLogin()`, `showForgot()`, `showResetCode()` hide it
- Signup handler collects metadata and passes `user_metadata` to `create-user` edge function
- Edge function (`supabase/functions/create-user/index.ts`) updated to accept and pass `user_metadata` to `supabase.auth.admin.createUser()`

### Task 2: Company Name to Org Matching
- In `onAuthSuccess()` org auto-detect block, after domain-based matching, added company-name matching
- If user still has no org and `user_metadata.company` is set, queries `organizations` table with `name=ilike.` match
- Auto-assigns org via PATCH to `user_profiles` if match found

### Task 3: Platform Admin User Detail — All Fields
- `admin-api` `list-users` action now returns `user_metadata` (full_name, company, phone, designation), `tier`, and `subscription_status`
- `renderPAUsersTable()` now shows: Email, Name, Company, Role, Organization, Tier, Subscription, Status, Created, Set Role (10 columns)
- Search filter now also searches by full_name
- `showPAUserDetail()` expanded to 3-column grid with all profile fields
- Added `_paSubBadge()` helper for subscription status badges (active=green, past_due=amber, cancelled=red, none/free=grey)
- User detail panel now loads and shows top 5 most-used pages from `_usageStats` in the user's JSONB blob

### Task 4: Lightweight Usage Tracking
- Added `_usageStats` global and `trackPageView(pageName)` function
- Wired into all 10 open functions: openG2M, openTodo, openPlans, openFeedback, openPrioritization, showDashboard, openInsights, openReports, openCapacity, openArtifacts
- `_usageStats` saved in `buildDataPayload()` and loaded in `applyDataPayload()`

### Task 5: Subscription Enforcement
- `loadUserTier()` now fetches `subscription_status` alongside tier
- If `subscription_status` is `past_due` or `cancelled`, forces `_userTier='basic'` and shows persistent banner
- `showSubscriptionBanner(status)` creates fixed banner at top:52px with payment/resubscribe links
- Dark mode support: dark amber bg for past_due, dark red bg for cancelled

### Task 6: Platform Admin Subscription View
- Subscription badges in user table and detail view via `_paSubBadge()`
- System Stats tab now shows: Paying Users count, Past Due count, Monthly Revenue estimate (standard=$17, pro=$25)
- Added "Users by Subscription Status" card with active/past_due/cancelled/free breakdown

---
